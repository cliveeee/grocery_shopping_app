import 'package:flutter/material.dart';

class AppColors {
  // Background Colors
  static const background = Color(0xFFF8F8F8);
  static const cardBackground = Color(0xFFFFFFFF);

  // Accent Colors

  //  (Dairy & Eggs)
  static const accentYellow = Color(0xFFFFF6E5);

  //  (Meat & Fish)
  static const accentPink = Color(0xFFFDEDED);

  // ( Beverages)
  static const accentBlue = Color(0xFFE5F0FF);

  //  (Fresh Fruits)
  static const accentGreen = Color(0xFFE8F7E5);

  // (Cooking Oil)
  static const accentPurple = Color(0xFFF5E8FF);

  // Text Colors
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF9E9E9E);

  // Button Colors
  static const buttonAdd = Color(0xFF27AE60);
  static const buttonFilter = Color(0xFF00897B);

  // Borders & Dividers
  static const divider = Color(0xFFE0E0E0);

  // Icons
  static const iconDefault = Color(0xFF9E9E9E);
  static const iconActive = Color(0xFF00897B);
}
